<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash Screen Theme - Using splashscreen.png as-is -->
    <style name="Theme.StudyChamp.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splashscreen</item>
        <item name="windowSplashScreenAnimationDuration">1500</item>
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
        <item name="postSplashScreenTheme">@style/Theme.Startup_hackathon20</item>
    </style>
</resources>
